.gradle/
build/
app/build/
local.properties
keystore.properties
*.iml
.idea/
.cxx/

# Per-environment secrets — templates (.env.*.template) are tracked, real
# values are not. See app/build.gradle.kts for how these are consumed.
.env.staging
.env.prod
