# Production environment — consumed by `./gradlew assembleRelease`.
# Copy this file to `.env.prod` (gitignored) and fill in real values.
# Every key must be present; a missing key fails the build by design.

# Base URL of the backend endpoint.
API_BASE_URL=
