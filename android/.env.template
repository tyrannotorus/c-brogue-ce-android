# Staging environment — consumed by `./gradlew assembleStaging`.
# Copy this file to `.env` (gitignored) and fill in real values.
# Every key must be present; a missing key fails the build by design.

# Base URL of the staging backend endpoint.
API_STAGING_URL=
